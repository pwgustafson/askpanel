# AskPanel

In-app help chat and guided feature requests for any web product, grounded only in a
markdown corpus you write. See `SPEC.md` for the design and `docs/` for the protocol,
configuration, and integration guides.

Under construction — v0.1 is being built against two host applications.
